# دليل اختبار Pull to Refresh (السحب للتحديث)

## كيفية التحقق من أن الميزة تعمل:

### 1. على الويب (Web):
- افتح Developer Console (F12)
- اذهب إلى الصفحة الرئيسية للعميل أو صفحة الرحلات للسائق
- **اسحب الصفحة للأسفل** (Pull down)
- راقب الـ Console للـ logs التالية:
  - `🔄 [Pull to Refresh] Customer home refresh started` (للعميل)
  - `🔄 [Pull to Refresh] Driver trips refresh started` (للسائق)
  - `✅ [Pull to Refresh] Refresh completed`
- يجب أن ترى toast message: "تم تحديث البيانات" أو "تم تحديث الطلبات"

### 2. على الموبايل (Native):
- افتح التطبيق
- اذهب إلى أي صفحة تحتوي على Pull to Refresh
- **اسحب الصفحة للأسفل** (Swipe down)
- يجب أن ترى مؤشر التحميل (spinner) في الأعلى
- بعد انتهاء التحديث، يجب أن تختفي الرسالة

## الصفحات التي تحتوي على Pull to Refresh:

### ✅ للعميل:
- الصفحة الرئيسية (`customer/home.tsx`)
- طلباتي (`customer/my-orders.tsx`)

### ✅ للسائق:
- الرحلات الجديدة (`driver/trips.tsx`)
- طلباتي (`driver/my-orders.tsx`)
- السجل (`driver/history.tsx`)
- المحفظة (`driver/wallet.tsx`)

### ✅ للإدارة:
- الطلبات (`admin/orders.tsx`)
- طلباتي (`admin/my-orders.tsx`)
- المستخدمين (`admin/users.tsx`)
- السائقين (`admin/drivers.tsx`)
- لوحة التحكم (`admin/dashboard.tsx`)
- المحاسبة (`admin/accounting.tsx`)

## ملاحظات مهمة:

1. **على الويب**: قد لا يعمل Pull to Refresh بشكل جيد في بعض المتصفحات. في هذه الحالة:
   - استخدم زر التحديث في `CurrentLocationDisplay` (للموقع)
   - أو أعد تحميل الصفحة يدوياً

2. **التحقق من الـ Logs**:
   - افتح Developer Console (F12)
   - ابحث عن logs تبدأ بـ `🔄 [Pull to Refresh]`
   - تأكد من ظهور `✅ [Pull to Refresh] ... completed`

3. **التحقق من Toast Messages**:
   - عند نجاح التحديث، يجب أن ترى toast message في الأعلى
   - عند فشل التحديث، يجب أن ترى toast error message

## استكشاف الأخطاء:

إذا لم يعمل Pull to Refresh:

1. **تحقق من الـ Console**:
   - ابحث عن أخطاء JavaScript
   - تحقق من أن `RefreshControl` مستورد بشكل صحيح

2. **تحقق من Platform**:
   - على الويب، قد تحتاج إلى استخدام حل بديل
   - على الموبايل، يجب أن يعمل بشكل طبيعي

3. **تحقق من ScrollView**:
   - تأكد من أن `ScrollView` يحتوي على `refreshControl` prop
   - تأكد من أن `refreshing` state يتم تحديثه بشكل صحيح

