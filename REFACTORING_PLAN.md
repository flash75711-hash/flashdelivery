# خطة إعادة هيكلة الكود - تبسيط صفحات "طلباتي"

## 🎯 الهدف:
- توحيد أسماء الصفحات: جميع الأدوار تستخدم `my-orders`
- تقليل تكرار الكود
- تحسين الأداء
- تسهيل الصيانة

---

## 📋 التغييرات المقترحة:

### 1. **توحيد أسماء الصفحات:**
   - ✅ العميل: `customer/orders` → `customer/my-orders`
   - ✅ السائق: `driver/my-orders` (موجود)
   - ✅ المدير: `admin/my-orders` (موجود)

### 2. **إنشاء Components مشتركة:**
   - `components/OrderCard.tsx` - بطاقة الطلب المشتركة
   - `components/OrderTypeCards.tsx` - بطاقات اختيار نوع الطلب (توصيل طلب / طلبات من بره)

### 3. **إنشاء Hooks مشتركة:**
   - `hooks/useMyOrders.ts` - hook لتحميل الطلبات مع Realtime subscription

### 4. **إنشاء صفحة مشتركة لعرض التفاصيل:**
   - `app/orders/[id].tsx` - صفحة تفاصيل الطلب المشتركة لجميع الأدوار

### 5. **تبسيط الصفحات:**
   - `customer/my-orders.tsx` - نفس تصميم `driver/my-orders` و `admin/my-orders`
   - نقل منطق التفاوض إلى صفحة التفاصيل المشتركة

---

## 🚀 الفوائد:

1. **تقليل تكرار الكود**: من ~600 سطر إلى ~200 سطر لكل صفحة
2. **سهولة الصيانة**: تغيير واحد يؤثر على جميع الصفحات
3. **تحسين الأداء**: استخدام React.memo و useMemo
4. **تجربة مستخدم أفضل**: تصميم موحد لجميع الأدوار
5. **كود أنظف**: فصل الاهتمامات (separation of concerns)

---

## 📝 الخطوات:

1. ✅ إنشاء `hooks/useMyOrders.ts`
2. ✅ إنشاء `components/OrderCard.tsx`
3. ✅ إنشاء `components/OrderTypeCards.tsx`
4. ✅ إنشاء `app/orders/[id].tsx`
5. ✅ إعادة تسمية `customer/orders.tsx` → `customer/my-orders.tsx`
6. ✅ تبسيط `customer/my-orders.tsx`
7. ✅ تحديث جميع المراجع

