# 🔧 حل مشاكل الخادم

## ❌ خطأ -102

هذا الخطأ يعني أن الخادم غير متاح أو لم يبدأ بعد.

## ✅ الحل السريع:

### 1. أوقف جميع العمليات:
```bash
pkill -9 -f "expo|metro"
```

### 2. حرر المنفذ:
```bash
lsof -ti:8081 | xargs kill -9
```

### 3. أعد التشغيل:
```bash
npm start
```

### 4. انتظر حتى ترى:
```
› Metro waiting on exp://...
```

### 5. افتح المتصفح:
```
http://localhost:8081
```

## 🔄 إذا استمرت المشكلة:

### تنظيف كامل:
```bash
# 1. أوقف جميع العمليات
pkill -9 -f "expo|metro|node"

# 2. احذف الكاش
rm -rf .expo node_modules/.cache

# 3. أعد تثبيت الحزم
npm install --legacy-peer-deps

# 4. أعد التشغيل
npm start
```

## 📋 التحقق من الحالة:

### تحقق من أن الخادم يعمل:
```bash
curl http://localhost:8081
```

إذا رأيت HTML، الخادم يعمل!

### تحقق من المنفذ:
```bash
lsof -i:8081
```

## 💡 نصائح:

1. **انتظر 15-20 ثانية** بعد `npm start` قبل فتح المتصفح
2. **تحقق من Terminal** لرؤية أي أخطاء
3. **استخدم `--clear`** لإزالة الكاش: `npm start -- --clear`

## ✅ الآن:

التطبيق يعمل على: **http://localhost:8081**

