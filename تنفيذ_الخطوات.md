# 🚀 تنفيذ الخطوات - دليل سريع

## ✅ الطريقة الأسهل والأضمن (موصى به)

### 📋 الخطوات (5 دقائق):

#### 1️⃣ افتح Supabase Dashboard
- اذهب إلى: https://supabase.com/dashboard
- سجل الدخول
- اختر مشروعك (أو أنشئ مشروع جديد)

#### 2️⃣ افتح SQL Editor
- من القائمة الجانبية: اضغط على **SQL Editor**
- اضغط على **New Query** (أو استخدم `Ctrl+K`)

#### 3️⃣ انسخ ملف SQL
- في هذا المشروع، افتح ملف: **`supabase_setup.sql`**
- انسخ **جميع** المحتوى:
  - اضغط `Ctrl+A` (تحديد الكل)
  - اضغط `Ctrl+C` (نسخ)

#### 4️⃣ الصق في SQL Editor
- في Supabase SQL Editor، اضغط `Ctrl+V` (لصق)
- يجب أن ترى جميع الاستعلامات (340 سطر)

#### 5️⃣ نفذ الاستعلامات
- اضغط على زر **Run** (أو `Ctrl+Enter`)
- انتظر حتى تكتمل (قد يستغرق 10-30 ثانية)
- يجب أن ترى رسالة "Success" ✅

#### 6️⃣ فعّل Realtime (مهم جداً!)
1. من القائمة الجانبية: **Database**
2. اضغط على **Replication**
3. ابحث عن الجداول التالية وفعّل Realtime لكل منها:
   - ✅ **orders** - اضغط على Toggle بجانبها
   - ✅ **profiles** - اضغط على Toggle بجانبها
   - ✅ **wallets** - اضغط على Toggle بجانبها

#### 7️⃣ تحقق من الجداول (اختياري)
- من القائمة: **Table Editor**
- يجب أن ترى 5 جداول:
  - `profiles`
  - `vendors`
  - `orders`
  - `wallets`
  - `driver_locations`

## 🎯 بعد التنفيذ

### إنشاء حساب مدير:
1. سجل حساب في التطبيق
2. في Supabase Dashboard → **Table Editor** → `profiles`
3. ابحث عن سجلك
4. عدّل حقل `role` إلى `admin`
5. احفظ

أو استخدم SQL:
```sql
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'your_email@example.com';
```

## 🚀 تشغيل التطبيق

```bash
npm start
```

## ✅ تم!

التطبيق جاهز الآن! 🎉

---

## 🔧 استكشاف الأخطاء

### مشكلة: "permission denied"
- تأكد من أنك تستخدم حساب المشروع الصحيح
- تحقق من أن RLS مفعل (يجب أن يكون مفعل تلقائياً)

### مشكلة: "relation already exists"
- هذا طبيعي إذا نفذت السكريبت مرتين
- يمكنك تجاهل هذه الرسائل

### مشكلة: Realtime لا يعمل
- تأكد من تفعيل Realtime في Database > Replication
- تأكد من أن الجدول موجود

## 📞 للمساعدة

- راجع `DATABASE_SETUP_STEPS.md` للتفاصيل
- راجع `AUTO_SETUP.md` للطرق الأخرى

