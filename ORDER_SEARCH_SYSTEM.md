# نظام البحث التلقائي عن السائقين

## 📋 نظرة عامة

نظام البحث التلقائي عن السائقين مع توسيع النطاق تدريجياً. يتم البحث عن السائقين في نطاق معين، وإذا لم يتم العثور على سائق يقبل الطلب، يتم توسيع النطاق تلقائياً.

---

## 🎯 الميزات

### 1. **البحث التلقائي التدريجي**
- البحث الأولي في نطاق 3 كم لمدة 10 ثواني
- إذا لم يتم العثور على سائق، توسيع النطاق إلى 6 كم لمدة 10 ثواني أخرى
- إيقاف البحث تلقائياً بعد انتهاء المدة
- إمكانية البحث مرة أخرى

### 2. **إعدادات قابلة للتعديل**
- جميع الإعدادات (المسافة والمدة) قابلة للتعديل من dashboard الإدارة
- الإعدادات محفوظة في قاعدة البيانات

### 3. **تطبيق على جميع أنواع الطلبات**
- **طلبات من خارج**: البحث عن السائقين بجانب أبعد مكان
- **توصيل طلب (بسيط)**: البحث عن السائقين من نقطة الاستلام
- **توصيل طلب (متعدد)**: البحث عن السائقين من نقطة الانطلاق

---

## 📁 الملفات المطلوبة

### 1. **قاعدة البيانات**
- `create_order_search_system.sql` - إنشاء الجداول والإعدادات

### 2. **Hooks**
- `hooks/useOrderSearch.ts` - Hook للبحث التلقائي

### 3. **Components**
- `components/OrderSearchStatus.tsx` - عرض حالة البحث

### 4. **صفحات العملاء**
- `app/customer/outside-order.tsx` - تحديث لاستخدام النظام الجديد
- `app/customer/deliver-package.tsx` - تحديث لاستخدام النظام الجديد

### 5. **Dashboard الإدارة**
- `app/(tabs)/admin/dashboard.tsx` - إضافة واجهة تعديل الإعدادات

---

## 🔧 خطوات التنفيذ

### 1. **تنفيذ SQL Script**
```sql
-- نفذ create_order_search_system.sql في Supabase SQL Editor
```

### 2. **تحديث الكود**
- سيتم تحديث `outside-order.tsx` و `deliver-package.tsx` لاستخدام النظام الجديد
- سيتم إضافة واجهة الإدارة لتعديل الإعدادات

---

## ⚙️ الإعدادات الافتراضية

| الإعداد | القيمة الافتراضية | الوصف |
|--------|-------------------|-------|
| `initial_search_radius_km` | 3 | نطاق البحث الأولي بالكيلومتر |
| `expanded_search_radius_km` | 6 | نطاق البحث الموسع بالكيلومتر |
| `initial_search_duration_seconds` | 10 | مدة البحث الأولي بالثواني |
| `expanded_search_duration_seconds` | 10 | مدة البحث الموسع بالثواني |

---

## 🔄 سير العمل

### **سيناريو 1: طلبات من خارج**

1. العميل يختار الأماكن والعناصر
2. يتم حساب أبعد مكان من موقع العميل
3. بدء البحث عن السائقين بجانب أبعد مكان:
   - **المرحلة 1**: نطاق 3 كم لمدة 10 ثواني
   - **المرحلة 2**: نطاق 6 كم لمدة 10 ثواني
4. إذا قبل سائق الطلب → إيقاف البحث
5. إذا انتهت المدة → إيقاف البحث مع إمكانية البحث مرة أخرى

### **سيناريو 2: توصيل طلب (بسيط)**

1. العميل يدخل نقطة الاستلام والتسليم
2. بدء البحث عن السائقين من نقطة الاستلام:
   - **المرحلة 1**: نطاق 3 كم لمدة 10 ثواني
   - **المرحلة 2**: نطاق 6 كم لمدة 10 ثواني
3. إذا قبل سائق الطلب → إيقاف البحث
4. إذا انتهت المدة → إيقاف البحث مع إمكانية البحث مرة أخرى

### **سيناريو 3: توصيل طلب (متعدد)**

1. العميل يدخل نقاط التوصيل المتعددة
2. بدء البحث عن السائقين من نقطة الانطلاق:
   - **المرحلة 1**: نطاق 3 كم لمدة 10 ثواني
   - **المرحلة 2**: نطاق 6 كم لمدة 10 ثواني
3. إذا قبل سائق الطلب → إيقاف البحث
4. إذا انتهت المدة → إيقاف البحث مع إمكانية البحث مرة أخرى

---

## 📱 واجهة المستخدم

### **عرض حالة البحث**
- شريط حالة يظهر:
  - حالة البحث الحالية (بحث أولي / بحث موسع / متوقف)
  - الوقت المتبقي
  - عدد السائقين الموجودين
- زر "البحث مرة أخرى" عند إيقاف البحث

---

## 🎨 تحسينات مقترحة

1. **إشعارات فورية**: إرسال إشعارات للسائقين عند بدء كل مرحلة بحث
2. **إحصائيات**: تتبع معدل نجاح البحث في كل نطاق
3. **تخصيص أكثر**: إضافة مراحل بحث إضافية (مثلاً: 3 كم → 6 كم → 10 كم)
4. **تحديثات Realtime**: استخدام Supabase Realtime لتحديث حالة البحث فوراً

---

**آخر تحديث**: يناير 2024



