# 📸 إعداد Catbox لرفع الصور

## ✅ ما تم تنفيذه

### 1. استخدام Catbox بدلاً من Supabase Storage
- ✅ تم إنشاء `lib/catbox.ts` لرفع الصور إلى Catbox
- ✅ تم تحديث `complete-registration/driver.tsx` لاستخدام Catbox
- ✅ لا حاجة لـ Supabase Storage bucket بعد الآن

### 2. إصلاح مشكلة Google Sign In
- ✅ بعد تسجيل الدخول بجوجل، يتم التحقق من إكمال التسجيل تلقائياً
- ✅ إذا لم يكمل المستخدم التسجيل، يتم توجيهه إلى شاشة إكمال التسجيل
- ✅ لا حاجة لإدخال الإيميل/كلمة المرور بعد Google Sign In

---

## 🔧 الإعداد

### 1. متغير البيئة
تم إضافة `EXPO_PUBLIC_CATBOX_USERHASH` في ملف `.env`:
```
EXPO_PUBLIC_CATBOX_USERHASH=15ba6cb45a67a6b5a87de54c6
```

### 2. كيفية العمل

#### رفع الصور:
```typescript
import { uploadImageToCatbox } from '@/lib/catbox';

const imageUrl = await uploadImageToCatbox(imageUri, 'filename');
// imageUrl سيكون رابط الصورة من Catbox
```

#### التحقق من إكمال التسجيل:
```typescript
import { isRegistrationComplete } from '@/lib/supabase';

const isComplete = await isRegistrationComplete(userId);
// true إذا أكمل التسجيل، false إذا لم يكمل
```

---

## 📋 التحقق من إكمال التسجيل

### للعميل (Customer):
- ✅ الاسم الكامل
- ✅ رقم التليفون
- ✅ عنوان واحد على الأقل

### للسائق (Driver):
- ✅ الاسم الكامل
- ✅ رقم التليفون
- ✅ صورة البطاقة الشخصية
- ✅ صورة سيلفي

### لمزود الخدمة (Vendor):
- ✅ اسم المكان
- ✅ الموقع (latitude & longitude)

---

## 🎯 تدفق تسجيل الدخول بجوجل

1. المستخدم يضغط "تسجيل الدخول بجوجل"
2. يتم فتح المتصفح لتسجيل الدخول
3. بعد الموافقة، يتم إنشاء حساب في Supabase
4. يتم إنشاء ملف أساسي في `profiles` (بدون بيانات كاملة)
5. يتم التحقق من إكمال التسجيل تلقائياً
6. إذا لم يكمل:
   - يتم توجيهه إلى شاشة إكمال التسجيل
7. إذا أكمل:
   - يتم توجيهه إلى الشاشة الرئيسية

---

## 💡 ملاحظات

- ✅ Catbox يوفر مساحة غير محدودة مجاناً
- ✅ الصور محمية بـ userhash
- ✅ لا حاجة لإعداد Storage bucket في Supabase
- ✅ الروابط دائمة ولا تنتهي صلاحيتها

---

## 🚀 جاهز للاستخدام!

بعد هذه التحديثات، النظام سيعمل بشكل كامل مع Catbox وبدون مشاكل في Google Sign In.

