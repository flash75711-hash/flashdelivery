# 🧪 اختبار لوحة السائق - دليل سريع

## ✅ الخطوات المطلوبة:

### 1. تأكد من أن السيرفر يعمل
- السيرفر يعمل على: `http://localhost:8081`
- إذا لم يكن يعمل، شغّله بـ: `npm start`

### 2. سجل الدخول كسائق
- افتح المتصفح: `http://localhost:8081`
- سجل دخول بحساب سائق (أو أنشئ حساب جديد واختر "سائق")
- إذا كان لديك حساب موجود، تأكد من أن `role = 'driver'` في جدول `profiles`

### 3. افتح صفحة لوحة السائق
- بعد تسجيل الدخول، اذهب إلى: `/driver/dashboard`
- أو اضغط على تبويب "لوحة السائق" في القائمة السفلية

### 4. اختبر زر التفعيل
- يجب أن ترى بطاقة "حالة السائق" في أعلى الصفحة
- اضغط على زر "تفعيل" (أزرق)
- يجب أن:
  - ✅ يتغير الزر إلى "إيقاف" (أحمر)
  - ✅ تظهر رسالة نجاح
  - ✅ يبدأ تتبع الموقع تلقائياً
  - ✅ يظهر الموقع الحالي مع العنوان والإحداثيات

### 5. اختبر زر الإيقاف
- اضغط على زر "إيقاف"
- يجب أن:
  - ✅ يتوقف تتبع الموقع
  - ✅ يختفي الموقع من الشاشة
  - ✅ تظهر رسالة نجاح

## 🔍 ما يجب التحقق منه:

### في Console (F12):
- يجب أن ترى رسائل:
  - `✅ Driver location updated: {lat, lon, address}`
  - `📍 Fetching location with high accuracy...`
  - `✅ Location fetched: {lat, lon, accuracy}`

### في قاعدة البيانات:
```sql
-- التحقق من حالة السائق
SELECT id, is_online FROM profiles WHERE role = 'driver';

-- التحقق من موقع السائق
SELECT driver_id, latitude, longitude, updated_at 
FROM driver_locations 
WHERE driver_id = 'your_driver_id' AND order_id IS NULL;
```

## ⚠️ في حالة وجود مشاكل:

### المشكلة: "يجب إضافة حقل is_online في قاعدة البيانات"
- **الحل:** تم تنفيذ SQL بالفعل، لكن إذا ظهرت هذه الرسالة:
  - افتح Supabase SQL Editor
  - نفذ: `ALTER TABLE profiles ADD COLUMN IF NOT EXISTS is_online BOOLEAN DEFAULT false;`

### المشكلة: لا يظهر الموقع
- **الحل:**
  - تأكد من السماح بالوصول إلى الموقع في المتصفح
  - تحقق من Console للأخطاء
  - تأكد من أن `getLocationWithHighAccuracy` يعمل بشكل صحيح

### المشكلة: الزر لا يعمل
- **الحل:**
  - تحقق من Console للأخطاء
  - تأكد من أن RLS policies تسمح بتحديث `is_online`
  - تحقق من أن `user` موجود في `useAuth`

## 📝 ملاحظات:
- تتبع الموقع يحدث كل 10 ثوانٍ عند التفعيل
- الموقع يُحذف من قاعدة البيانات عند الإيقاف
- يمكن للسائق تفعيل/إيقاف حالته في أي وقت
