# 🧪 اختبار Bottom Navigation Bar

## ✅ الخطوات:

### 1. افتح التطبيق من الصفحة الرئيسية:
```
http://localhost:8081/(tabs)
```

### 2. سجل دخول كسائق:
- رقم الهاتف: `02222222222`
- OTP: `999999` (بعد إضافة Test Phone Number في Supabase)

### 3. بعد تسجيل الدخول:
- سيتم التوجيه تلقائياً إلى: `/(tabs)/driver/dashboard`
- **يجب أن ترى الـ bottom navbar في الأسفل** مع 4 تبويبات:
  - 🏠 لوحة التحكم
  - 🗺️ الرحلات الجديدة
  - 💰 المحفظة
  - ⏰ سجل الرحلات

### 4. اختبر التنقل:
- اضغط على أي تبويب في الـ bottom navbar
- يجب أن تنتقل الصفحة ويبقى الـ navbar ظاهراً
- التبويب النشط يجب أن يكون أزرق (`#007AFF`)
- التبويبات غير النشطة رمادية (`#999`)

---

## 📱 التبويبات لكل دور:

### **العميل (Customer)** - 3 تبويبات:
- 🏠 الرئيسية
- 📋 الطلبات
- 👤 الملف الشخصي

### **السائق (Driver)** - 4 تبويبات:
- 🏠 لوحة التحكم
- 🗺️ الرحلات الجديدة
- 💰 المحفظة
- ⏰ سجل الرحلات

### **مزود الخدمة (Vendor)** - 2 تبويبات:
- 🏪 المتجر
- 👤 الملف الشخصي

### **المدير (Admin)** - 5 تبويبات:
- 📊 لوحة التحكم
- 👥 السائقين
- 💵 المحاسبة
- 📋 الطلبات
- 📍 الأماكن

---

## 🔍 ما يجب أن تراه:

### التصميم:
- ✅ شريط أبيض في الأسفل
- ✅ ارتفاع 60px
- ✅ ظل خفيف في الأعلى
- ✅ خط فاصل في الأعلى
- ✅ أيقونات ونصوص واضحة

### الألوان:
- ✅ التبويب النشط: أزرق (`#007AFF`)
- ✅ التبويبات غير النشطة: رمادي (`#999`)
- ✅ الخلفية: أبيض (`#fff`)

---

## ⚠️ ملاحظات:

- الـ navbar يظهر فقط عند فتح الصفحات من خلال الـ tabs navigation
- إذا فتحت صفحة مباشرة عبر URL (مثل `/driver/history`)، قد لا يظهر الـ navbar
- تأكد من فتح الصفحة من: `/(tabs)/driver/history` وليس `/driver/history`

---

## 🐛 في حالة عدم الظهور:

1. **تأكد من أنك داخل الـ tabs:**
   - URL يجب أن يبدأ بـ `/(tabs)/`
   - مثال: `http://localhost:8081/(tabs)/driver/dashboard`

2. **أعد تحميل الصفحة:**
   - اضغط `Ctrl + R` أو `F5`

3. **تحقق من Console:**
   - افتح Developer Tools (`F12`)
   - ابحث عن أخطاء في Console

4. **تأكد من تسجيل الدخول:**
   - يجب أن تكون مسجل دخول كسائق
   - تحقق من أن `user.role === 'driver'`
