# اختبار سريع للإشعارات

## 🚀 خطوات الاختبار السريع

### 1. تشغيل السيرفر
```bash
cd /home/zero/Desktop/flash
npm start
```

### 2. فتح المتصفح
افتح `http://localhost:8081` في المتصفح

### 3. تسجيل الدخول
- سجل دخول كعميل أو سائق

### 4. اختبار الإشعارات

#### أ. اختبار من Console المتصفح:
افتح Developer Console (F12) ثم نفذ:

```javascript
// اختبار إشعار للعميل الحالي
await testNotifications.testCustomerNotification();

// اختبار إشعار لسائق (استبدل DRIVER_ID)
await testNotifications.testDriverNotification('DRIVER_ID_HERE');

// اختبار إشعار مرتبط بطلب
await testNotifications.testOrderNotification('USER_ID', 'ORDER_ID');
```

#### ب. اختبار من الواجهة:
1. **للعملاء**: أنشئ طلب جديد من `orders/deliver-package.tsx`
   - يجب أن يظهر إشعار: "تم إنشاء الطلب بنجاح"

2. **للسائقين**: 
   - أنشئ طلب من حساب عميل آخر
   - يجب أن يظهر إشعار: "طلب جديد متاح" (Web فقط)

3. **اختبار NotificationCard**:
   - اذهب إلى `customer/home.tsx` أو `driver/dashboard.tsx`
   - يجب أن يظهر كارت الإشعارات في أعلى الصفحة
   - النقر على الكارت يفتح Modal مع جميع الإشعارات

---

## ✅ النتائج المتوقعة

### FloatingNotification (إشعارات عائمة):
- ✅ تظهر في أعلى الشاشة
- ✅ تختفي تلقائياً بعد بضع ثوانٍ
- ✅ يمكن إغلاقها يدوياً
- ✅ تُحدّث كمقروءة عند الإغلاق

### NotificationCard:
- ✅ يظهر في `customer/home.tsx`
- ✅ يظهر في `driver/dashboard.tsx`
- ✅ يعرض عدد الإشعارات غير المقروءة
- ✅ النقر يفتح Modal مع جميع الإشعارات

---

## 🐛 حل المشاكل

### الإشعارات لا تظهر:
1. تأكد من تفعيل Realtime في Supabase Dashboard
2. تأكد من أن المستخدم مسجل دخول
3. تحقق من Console للأخطاء

### إشعارات الطلبات للسائقين لا تظهر:
- ✅ تم إصلاحها - يجب أن تظهر الآن

---

**تاريخ الإنشاء**: $(date)

