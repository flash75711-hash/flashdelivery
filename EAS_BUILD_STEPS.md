# 🚀 خطوات EAS Build - جاهز للتنفيذ

## ✅ تم الإعداد المبدئي!

تم إنشاء ملف `eas.json` مع 3 ملفات تعريف (profiles):

1. **development** - للتطوير والاختبار
2. **preview** - للمعاينة قبل النشر
3. **production** - للنشر النهائي

---

## 📝 الخطوات التالية:

### الخطوة 1: إنشاء حساب Expo (إذا لم يكن لديك)

افتح المتصفح واذهب إلى:
```
https://expo.dev/signup
```

أو سجل دخول إذا كان لديك حساب:
```
https://expo.dev/login
```

---

### الخطوة 2: تسجيل الدخول من Terminal

```bash
cd /home/zero/.cursor/worktrees/flash/sai
npx eas-cli login
```

سيطلب منك:
```
✔ Email or username: أدخل البريد الإلكتروني أو اسم المستخدم
✔ Password: أدخل كلمة المرور
```

---

### الخطوة 3: ربط المشروع بحسابك

```bash
npx eas-cli build:configure
```

سيسأل:
```
✔ What would you like your Android package name to be?
  اترك الافتراضي: com.flash.delivery
  أو غيّره إذا أردت
```

---

### الخطوة 4: اختيار نوع البناء

#### للتطوير والاختبار السريع:
```bash
npx eas-cli build --platform android --profile development
```

#### للنشر والاستخدام الفعلي:
```bash
npx eas-cli build --platform android --profile production
```

#### للمعاينة:
```bash
npx eas-cli build --platform android --profile preview
```

---

### الخطوة 5: انتظر اكتمال البناء

```
⏳ Queued build...
⏳ Building...
   سيستغرق 10-20 دقيقة
   
✅ Build finished!
   
   Build URL: https://expo.dev/accounts/[username]/projects/flash-delivery/builds/[id]
```

---

### الخطوة 6: تحميل APK

#### الطريقة 1: من الرابط
انسخ الرابط من Terminal وافتحه على هاتفك

#### الطريقة 2: من لوحة التحكم
```
https://expo.dev/accounts/[username]/projects/flash-delivery/builds
```

#### الطريقة 3: مباشرة من Terminal
```bash
# بعد انتهاء البناء، سيعطيك خيار:
Download build artifact? (Y/n)
اضغط Y
```

---

### الخطوة 7: تثبيت على الهاتف

#### على الهاتف مباشرة:
1. افتح الرابط على الهاتف
2. اضغط "Download"
3. بعد التحميل، اضغط على الملف
4. اسمح بـ "Install from unknown sources" إذا طُلب
5. اضغط "Install"
6. جاهز! 🎉

#### عبر USB:
```bash
# بعد تحميل APK على الكمبيوتر:
adb install path/to/downloaded/app.apk
```

---

## 🎯 الأوامر الجاهزة

### كل الأوامر بالترتيب:

```bash
# 1. الانتقال للمشروع
cd /home/zero/.cursor/worktrees/flash/sai

# 2. تسجيل الدخول (مرة واحدة فقط)
npx eas-cli login

# 3. إعداد المشروع (مرة واحدة فقط)
npx eas-cli build:configure

# 4. بناء APK (اختر واحد):

# للتطوير:
npx eas-cli build --platform android --profile development

# للإنتاج:
npx eas-cli build --platform android --profile production

# للمعاينة:
npx eas-cli build --platform android --profile preview
```

---

## 📊 الفرق بين الملفات التعريفية (Profiles)

| الميزة | development | preview | production |
|--------|-------------|---------|------------|
| **الغرض** | التطوير والاختبار | المعاينة | النشر النهائي |
| **الحجم** | كبير | متوسط | محسّن |
| **Debugging** | ✅ مفعّل | ⚠️ محدود | ❌ معطّل |
| **السرعة** | عادي | سريع | أسرع |
| **للمستخدمين** | ❌ | ⚠️ | ✅ |

---

## 💡 نصائح مهمة

### 1. البناء الأول:
- قد يستغرق أطول (15-25 دقيقة)
- البناءات التالية أسرع (10-15 دقيقة)

### 2. الكوتا المجانية:
- **30 build مجاني/شهر**
- بعدها: اشتراك مدفوع ($29/شهر)

### 3. التحديثات:
- يمكنك بناء APK جديد في أي وقت
- كل build له رقم مميز

### 4. مراقبة البناء:
```bash
# لعرض جميع البناءات:
npx eas-cli build:list

# لعرض حالة بناء معين:
npx eas-cli build:view [BUILD_ID]
```

---

## ⚠️ إذا واجهت مشاكل

### "No Expo account found"
```bash
# سجل دخول أولاً:
npx eas-cli login
```

### "Project not configured"
```bash
# أعد التكوين:
npx eas-cli build:configure
```

### "Build failed"
```bash
# تحقق من الأخطاء:
npx eas-cli build:view [BUILD_ID]

# أعد المحاولة:
npx eas-cli build --platform android --profile development --clear-cache
```

### "Cannot install APK"
على الهاتف:
- Settings > Security
- فعّل "Unknown sources"
أو:
- Settings > Apps > Special access
- Install unknown apps
- اختر المتصفح أو File Manager
- السماح

---

## 🎨 تخصيص قبل البناء (اختياري)

### تغيير اسم التطبيق:
```bash
nano app.json
# غيّر "name": "Flash Delivery"
```

### تغيير الأيقونة:
```bash
# استبدل الملفات:
assets/icon.png (1024x1024)
assets/adaptive-icon.png (1024x1024)
```

### تغيير Splash Screen:
```bash
# استبدل:
assets/splash.png
```

---

## 📱 بعد التثبيت

### التحقق من عمل التطبيق:

✅ **اختبر:**
- تسجيل الدخول
- إنشاء طلب
- الإشعارات
- تحديد الموقع
- رفع الصور

✅ **تحقق من الأداء:**
- سرعة الفتح
- سلاسة التنقل
- استهلاك البطارية

---

## 🔗 روابط مفيدة

- **لوحة التحكم:** https://expo.dev/
- **البناءات:** https://expo.dev/builds
- **الوثائق:** https://docs.expo.dev/build/introduction/
- **الدعم:** https://forums.expo.dev/

---

## 🎯 التوصية

### للمبتدئين:
ابدأ بـ **development profile** للاختبار أولاً.

### للنشر:
استخدم **production profile** وتأكد من اختبار كل شيء.

---

## ✅ الخطوة التالية

**الآن افتح Terminal ونفّذ:**

```bash
cd /home/zero/.cursor/worktrees/flash/sai
npx eas-cli login
```

**بعد تسجيل الدخول:**

```bash
npx eas-cli build --platform android --profile development
```

---

**🎉 جاهز! ابدأ الآن!** 🚀📱

**⏱️ الوقت المتوقع: 15-20 دقيقة للبناء الأول**











