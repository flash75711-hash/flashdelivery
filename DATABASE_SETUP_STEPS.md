# 🗄️ خطوات إعداد قاعدة البيانات - خطوة بخطوة

## 📋 الخطوات السريعة

### 1. افتح Supabase Dashboard
- اذهب إلى [supabase.com/dashboard](https://supabase.com/dashboard)
- اختر مشروعك أو أنشئ مشروع جديد

### 2. افتح SQL Editor
- من القائمة الجانبية، اختر **SQL Editor**
- اضغط على **New Query**

### 3. انسخ وألصق الكود
- افتح ملف `supabase_setup.sql` في هذا المشروع
- انسخ **جميع** المحتوى
- الصقه في SQL Editor في Supabase

### 4. نفذ الاستعلامات
- اضغط على **Run** أو `Ctrl+Enter`
- انتظر حتى تكتمل جميع الاستعلامات
- يجب أن ترى رسالة "Success"

### 5. تفعيل Realtime (مهم جداً!)
1. من القائمة الجانبية، اختر **Database**
2. اختر **Replication**
3. فعّل Realtime للجداول التالية:
   - ✅ `orders`
   - ✅ `profiles`
   - ✅ `wallets`

### 6. التحقق من الجداول
- اذهب إلى **Table Editor**
- يجب أن ترى الجداول التالية:
  - `profiles`
  - `vendors`
  - `orders`
  - `wallets`
  - `driver_locations`

## ✅ التحقق من الإعداد

### اختبار الجداول:
```sql
-- التحقق من وجود الجداول
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('profiles', 'vendors', 'orders', 'wallets', 'driver_locations');
```

### اختبار السياسات:
```sql
-- التحقق من تفعيل RLS
SELECT tablename, rowsecurity 
FROM pg_tables 
WHERE schemaname = 'public' 
AND tablename IN ('profiles', 'vendors', 'orders', 'wallets', 'driver_locations');
```

## 🎯 إنشاء حساب مدير

بعد تسجيل حساب في التطبيق، قم بتحديث دوره:

```sql
-- استبدل 'your_email@example.com' ببريدك الإلكتروني
UPDATE profiles 
SET role = 'admin' 
WHERE email = 'your_email@example.com';
```

## 🔧 استكشاف الأخطاء

### مشكلة: "permission denied"
- تأكد من أنك تستخدم Service Role Key في SQL Editor
- أو استخدم Authentication > Policies للتحقق

### مشكلة: "relation does not exist"
- تأكد من تنفيذ جميع الاستعلامات
- تحقق من أن الجداول موجودة في Table Editor

### مشكلة: Realtime لا يعمل
- تأكد من تفعيل Realtime في Database > Replication
- تحقق من أن الجدول موجود في القائمة

## 📝 ملاحظات مهمة

1. **النسخ الاحتياطي**: احفظ ملف `supabase_setup.sql` كنسخة احتياطية
2. **البيئة**: تأكد من أنك تعمل على المشروع الصحيح في Supabase
3. **الأمان**: جميع السياسات (RLS) مفعلة لحماية البيانات

## ✅ بعد الإعداد

بعد إكمال جميع الخطوات:
1. ✅ الجداول منشأة
2. ✅ السياسات مفعلة
3. ✅ Realtime مفعل
4. ✅ جاهز للاستخدام!

الآن يمكنك تشغيل التطبيق:
```bash
npm start
```

