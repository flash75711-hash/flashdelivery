# 🔧 حل مشكلة الاتصال بـ Supabase

## 🔍 المشكلة

عند محاولة الاتصال بقاعدة البيانات، يظهر الخطأ:
```
connect ENETUNREACH 2a05:d018:...:5432
```

هذا يعني أن النظام يحاول الاتصال عبر **IPv6** لكن الاتصال يفشل.

## ✅ الحلول

### الحل 1: استخدام Supabase Dashboard (الأسهل والأضمن) ⭐

**هذا هو الحل الموصى به!**

1. افتح [Supabase Dashboard](https://supabase.com/dashboard)
2. اذهب إلى **SQL Editor**
3. انسخ ملف `supabase_setup.sql` والصقه
4. اضغط **Run**

**لماذا هذا الحل أفضل:**
- ✅ لا يحتاج اتصال مباشر من جهازك
- ✅ يعمل من المتصفح مباشرة
- ✅ أكثر أماناً
- ✅ لا يحتاج إعدادات شبكة معقدة

### الحل 2: إصلاح إعدادات IPv6

إذا أردت استخدام السكريبتات:

#### على Linux:
```bash
# تعطيل IPv6 مؤقتاً
sudo sysctl -w net.ipv6.conf.all.disable_ipv6=1
sudo sysctl -w net.ipv6.conf.default.disable_ipv6=1

# ثم شغّل السكريبت
node run_setup.js
```

#### أو إجبار استخدام IPv4 في السكريبت:
تم تحديث السكريبتات لتستخدم IPv4 تلقائياً.

### الحل 3: استخدام VPN أو Proxy

إذا كان هناك قيود على الشبكة:
- استخدم VPN
- أو استخدم Proxy

### الحل 4: التحقق من إعدادات Firewall

```bash
# التحقق من أن المنفذ 5432 مفتوح
sudo ufw allow 5432/tcp
```

## 🎯 التوصية

**استخدم Supabase Dashboard** - هذا هو الحل الأسهل والأكثر موثوقية!

### الخطوات:
1. افتح: https://supabase.com/dashboard
2. SQL Editor → New Query
3. انسخ `supabase_setup.sql`
4. الصق واضغط Run

## 📋 ملفات مساعدة

- **`تنفيذ_الخطوات.md`** - دليل مفصل للتنفيذ عبر Dashboard
- **`QUICK_EXECUTE.md`** - دليل سريع (3 خطوات)
- **`test_connection.js`** - لاختبار الاتصال

## ✅ بعد التنفيذ

بعد إنشاء الجداول:
1. فعّل Realtime: Database → Replication
2. فعّل: `orders`, `profiles`, `wallets`

## 💡 نصيحة

إذا استمرت المشكلة، استخدم **Supabase Dashboard** - إنه أسهل وأكثر موثوقية!

